
// This file re-exports all mock data from specialized files
// for backwards compatibility

export { dealCategories, mockDeals } from './mockDeals';
export { currentUser, mockUser, mockBusinessUser } from './mockUsers';
export { mockNotifications } from './mockNotifications';
export { formatDistanceToNow, formatTimeRemaining, getExpiryColor } from './mockHelpers';
